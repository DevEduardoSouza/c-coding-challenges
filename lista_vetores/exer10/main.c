#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char const *argv[])
{
    char nome[30];

    printf("Informe seu nome: ");
    gets(nome);

    for (int i = 0; i < 30; i++)
    {
        if (nome[i] == strupr)
        {
            printf("IHHEEEEE!!!");
        }
        
    }
    
    
    return 0;
}
